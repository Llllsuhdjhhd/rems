# **REMS 递归演化记忆系统规范（完整工程版）**

本文是在原始核心规范基础上，融合了面向生产环境的动态信念修正、情感遗忘模型、上下文防溢出策略、多场景输出适配以及记忆重组机制的完整工程化技术白皮书。

## **1\. 基本事件定义**

基本事件（Basic Event）是 REMS 中承载客观事实的最小记忆单元。基于最新的架构逻辑，基本事件不仅记录事实片段，还承载角色在当次事件中的瞬时状态（快照与情感），确保事件的可验证性与主体交互的完整性。

### **1.1 核心属性解析**

#### **1.1.1 唯一标识与元数据**

* event\_id：全局唯一的字典序标识，确保系统内所有事件在全局范围内的唯一性、可区分性与可追溯性。  
* create\_time：记录事件封装完成的绝对时刻。

#### **1.1.2 原始数据属性**

原始数据是基本事件的唯一数据底库，作为记忆系统生成、演化与追溯的绝对事实基准。

* content\_raw：即 L0 级原始文本。  
  * 基本事件：严格等于经剥离并判定为已完成的片段。  
  * 抽象事件：由子事件的中位摘要合集集成生成的合成事实描述。

#### **1.1.3 递归摘要体系属性**

* summaries：以字典形式存储各级递归摘要（L1\~Ln）。支持“固定比例压缩”“关键信息提取”或“语义改写”等模式。L1 级实现最大化的“保真压缩”，高阶层级呈现渐进式的抽象梯度。  
* summary\_lengths：以字典形式存储各级摘要对应的字符数。  
* actual\_max\_level：受动态熔断机制约束的实际生成层级。

#### **1.1.4 角色交互与瞬时状态属性**

通过 role\_list 记录当次事件中所有参与者的交互细节及量化快照。

* role\_list  
  * role\_id：关联至角色系统的唯一 ID。  
  * importance：S/A/B/C/D 五级分级。  
  * role\_snapshot：包含 L1（提及快照）、L2（互动快照）与 L3（决策快照）。  
  * emotional\_model：  
    * 五受量化（Vedana）：乐、苦、喜、忧、舍。  
    * 六根本烦恼量化（Klesha）：贪、嗔、痴、慢、疑、恶见。  
    * **工程附加逻辑**：角色受到强烈的情感感受时（如受与烦恼的剧烈波动），系统将为该事件赋予更高的记忆初始值与激活能量。  
  * **情感量化案例**：例如事件“用户心爱的宠物狗走失”，系统量化输出为 {"Vedana": {"苦": 0.9, "忧": 0.8}, "Klesha": {"贪(执念)": 0.7}}。该高分值将直接赋予此事件极高的抗遗忘权重。

#### **1.1.5 抽象演化与生命周期标记**

* is\_abstract：架构级常量。基本事件固定为 False，抽象事件固定为 True。  
* is\_abstracted：演化状态位，记录该事件是否已被高阶认知逻辑吸收。  
* status：反映事件在回忆系统中的权重（未闭环 / 活跃 / 静默）。

#### **1.1.6 扩展属性与规律内核**

* decoration：针对 content\_raw 的非事实性描述，支撑“梦见”系统。  
* insight：基本事件固定为 None；抽象事件存储实质性的规律见解或逻辑推导结论。

#### **1.1.7 事件聚合与长度约束属性**

* event\_length：记录 L0 级原始文本的字符长度。  
* **物理防御约束**：最高上限设定为 1/66 \* Context\_Window。  
* **工程化语义聚合（防碎片化机制）**：对于极其琐碎的短小动作，系统需具备上下文聚合能力，将细粒度连贯动作归于同一个事件中。  
  * **判定案例**：当输入为\*“我去跑了步，然后就回家了。回家后我打扫了卫生，大概1小时后，看了一会儿小说”\*。系统不应将其拆分为四个极短的微小事件，而应在缓冲区聚合封装为单一基本事件："content\_raw": "用户去跑步后回家，依次打扫了卫生并阅读了一小时小说。"

#### **1.1.8 抽象事件专用扩展属性**

* abstraction\_level：记录该事件在递归演化树中的深度。  
* source\_events：存储支撑该抽象节点生成的全部子事件 ID 列表。

## **2\. 角色系统定义与动态遗忘机制**

角色系统（Role System）的核心职能是维护全局唯一的角色标识（role\_id），并将分散在不同基本事件中的事实按时间顺序收集，形成角色的状态机。

### **2.1 角色标识与独立性**

* 角色 ID（role\_id）是持久化的“容器指针”，用于归集实体行为记录。  
* 去代词化映射：强制要求在输入转化为基本事件时，通过 role\_id 替换模糊代词。

### **2.2 角色白描系统（Chronological White-painting）**

* **收集与汇编**：每当基本事件包含 role\_id，系统便将关于该角色的“瞬时记录”拉取到白描轴中，按 create\_time 升序排列。  
  * **白描流水案例**：  
    * ROL-001 (用户) 早上 8 点购买了一杯美式咖啡。  
    * ROL-001 抱怨今天的咖啡口感过酸。  
* **白描的线性控制与动态遗忘机制**：针对长周期运行中白描轨迹的线性爆炸问题，系统采用动态遗忘机制而非物理硬截断。  
  * 角色的遗忘衰减与情感分析紧密关联：带有强烈情感感受（Vedana/Klesha 阈值高）的事件具有极高的记忆初始值，不易被遗忘；而低情感波动的日常琐事会加速衰减。  
  * 遗忘系数支持根据物理设备（如极大的存储空间与强算力）或特定业务需求进行全局配置，高要求的场景可显著降低遗忘速度。

### **2.3 双轨制认知状态机与语义卡片**

* 为彻底根治线性白描带来的检索稀释，系统建立双轨制：在时间线白描外，维护“高密度语义状态卡片”。  
* 该卡片存储极致压缩的键值对。  
  * **状态卡片案例**：系统将成百上千条白描流水在后台压缩为卡片词典：{"Role\_ID": "ROL-001", "核心偏好": \["深烘焙咖啡", "抗拒酸味"\], "性格均值": {"易激惹度": 0.2}}。在交互中，系统优先拉取该卡片提供绝对基准。

## **3\. 抽象事件定义与防幻觉规范**

抽象事件（Abstract Event）通过对多个低阶事件的归纳，形成具备跨时间属性的高阶认知节点。

### **3.1 核心属性解析**

与基本事件保持结构一致，但 content\_raw 为大模型基于关联事件集（source\_events）生成的合成事实，is\_abstract 固定为 True。

### **3.2 抽象事件的触发机制**

* **召回聚集触发**：某逻辑点召回历史事件超过 5 个，自动启动归并分析生成抽象事件。  
* **演化驱动触发**：定期扫描未被吸收的基本事件。

### **3.3 幻觉控制与防强化循环**

* **危险的自我强化循环规避**：如果系统仅用于后台记录（无需通过记忆进行预测或对话输出），则不存在该问题。  
* 若应用于多轮交互场景，为了防止抽象推演结论被反复当作原始事实入库造成幻觉闭环，系统在后续演化时，将按一定概率强制调用底层事件的“上一级摘要”（而非完全依赖当前的合成文本）进行递归抽象，以确保持续锚定客观事实。

## **4\. 事件代谢素材、触发机制与回忆提取规范**

### **4.1 残影（Shadow）与未完成事件的精准剥离**

* **残影**：存留在系统缓冲区内、未经整理的原始文本素材。  
* **未完成事件库**：存储已开启但未闭环的逻辑对象。  
* **复杂输入的精准剥离案例**：  
  * 当系统接收到输入：*“我去跑了步，然后就回家了。回家后我打扫了卫生，然后继续进行A项目的研发，大概1小时后，看了一会儿小说”*。  
  * 判定与剥离逻辑：大模型扫描到“A项目的研发”时，通过检索命中未完成事件库中的历史挂起事件（例如几天前开启的 A 项目逻辑实体）。此时，系统必须精准划开边界：将“打扫卫生”与“看小说”剥离出来作为全新的基本事件封装；将“继续研发A项目 1小时”单独抽离，归入库中已存在的 A 项目未完成事件，更新其完成进度与状态。这种分离机制保证了宏观长期任务与微观即时琐事的物理隔离。

### **4.2 事件的模糊截断与触发**

* **语义与长度触发**：当完整事实闭环，或累积长度达到 1/66 时触发封装。  
* **中间模糊截断机制**：在大模型引导下寻找语义断点，执行约 0.8 : 0.2 的比例截断。  
  * 工程化说明：此比例不需要绝对精确，只需在阈值附近寻找相对合理的句法停顿即可。因为即使在物理上被生硬截断，最终也可通过后续高阶抽象记忆的重新挂载，让事件在逻辑链条上恢复连贯。截断出的前部分封装入库，剩余部分滞留残影。

### **4.3 动态信念修正与知识冲突处理**

在长期记忆中，“知识冲突”不可避免，系统将其区分为两种底层逻辑进行处理：

1. **事实的自然演进**：有些冲突本身就是事物随时间发展的客观事实（如从喜欢苹果变成对苹果过敏）。此类信息不视为逻辑错误，系统依靠白描时间戳作为最新状态追加，依靠遗忘机制使旧状态自然衰退。  
2. **技术性与代差修正**：有些冲突是技术原因导致的（例如此时的模型推导结论不如将来的模型版本先进）。针对此类认知冲突，仲裁机制应赋予“未来时间点/更先进模型的判断”以绝对的采信权重，直接对旧有认知执行逻辑覆写（Tombstone 墓碑化）。

### **4.4 记忆召回机制（Recall）与重组抽象触发（Reconsolidation）**

回忆块（Recall Block）不仅是向下索取信息的数据包，更是向上演化抽象规律的驱动引擎。

* **回忆的执行方式**：  
  当接收到新输入时，系统以当前输入（Current Input）和残影（Shadow）为查询词（Query）。在底层向量数据库中，综合计算“语义相似度（Cosine Similarity） \+ 时间衰减权重 \+ 事件情感能量（AE）”，最终按综合打分提取高维匹配的 content\_raw 与角色状态卡片，并将其长度严格压缩至 1/6.6 上下文主权约束之内。  
* **回忆触发抽象机制（Memory Reconsolidation）**：  
  在工程实践中，**回忆是触发抽象事件产生最核心、甚至在多数情况下唯一的途径**。当多个相关的历史基本事件被同一语境重新激活并调取入“回忆块”时，这些记忆进入一种不稳定的“重组（Reconsolidation）”状态。  
  * **触发动作**：系统在生成回应的同时，后台异步核查此次提取的同一主题基本事件数量（如 ≥5 个）。此时，大模型利用这批被重新唤醒的记忆组合，强行进行归纳推理，从而生成一条全新的 is\_abstract \== True 的高阶抽象规律（Insight）并落库。

## **5\. 系统输出机制与多场景架构适配**

REMS 作为一个底层代谢架构，不强制绑定单一的响应模式。系统根据不同的商业与应用需求，灵活调整“代谢素材包（回忆块 \+ 残影 \+ 当前输入）”的处理管线与输出形态。

### **5.1 场景一：AI 互动对话伴侣（Interactive Dialogue）**

这是标准的强输出交互场景（如心理辅导数字人、私人 AI 助理）。

* **拼接与输出逻辑**：系统必须在毫秒级时间内组装完整的 Context Package（即：回忆块 \+ 语义卡片 \+ 残影 \+ 最新用户输入）。大模型以此为 Prompt，向用户输出符合角色当前情感量化状态的自然语言回复。  
* **后台异步代谢**：在生成回复后，系统在后台对刚才的对话过程进行残影剥离、摘要计算与基本事件的封装入库，不阻塞前台的用户对话体验。

### **5.2 场景二：个人生活录音与被动日志提取（Passive Logging）**

应用于如穿戴设备录音记录、长期会议监听或个人日志辅助等“只记不说”的场景。

* **拼接与输出逻辑**：该场景下**不需要对输入内容提供任何外部文本输出**。系统处于“静默倾听”状态。  
* **后台执行管线**：不存在前台回复步骤。接收到长音频转写的文本流后，系统将其直接送入残影缓冲区，达到 len\_msg 阈值后自动触发切分、剔除冗余口水话、提取事实生成基本事件、更新时间线白描。在此场景下，REMS 纯粹扮演一个高维度的“自治数据库”，为用户提供未来可能发起的检索服务储备客观事实。

### **5.3 场景三：生成式虚拟沙盒与复杂 NPC 生态（Generative Agents）**

应用于开放世界游戏或多智能体模拟环境。

* **拼接与输出逻辑**：环境事件（如“玩家进入酒馆”）作为输入，触发现场所有 NPC 的回忆检索。此时的输出不再是单纯的“对话”，而是被隐性指令转化器（Directive Parser）翻译为具体的**状态机改变或寻路决策**。  
* **执行案例**：系统拼接 回忆块 (玩家曾在村口伤害过该 NPC) \+ 当前输入 (玩家靠近)，输出结果直接转化为 NPC 系统底层的行为代码（如 Action: Flee 或 Action: Attack），并同步提升该 NPC 的 Klesha\_嗔 维度指标，整个过程无需任何人类可见的文本对话生成。